#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
	int a,b;
	cin >> a >> b;
	if (a>b)
	{
		printf("max=%d",a);
	}else{
		printf("max=%d",b);
	}
	return 0;
}